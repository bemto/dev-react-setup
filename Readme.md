# Bemto Dev React Setup

Basically, a common deps list for dev deps of react bemto projects.

In the future could potentially contain setup scripts for all other files like `.eslintrc`, `.babelrc` etc.
